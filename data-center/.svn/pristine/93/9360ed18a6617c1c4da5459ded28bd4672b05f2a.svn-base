package com.taomee.bigdata.task.query.update;

class LevelInfo extends UserInfo {
    protected BasicValue<Integer> level = new BasicValue<Integer>();

    protected boolean changed() {
        return level.changed();
    }

    protected void clear() {
        level.clear();
    }
}
