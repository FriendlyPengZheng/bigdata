后台根据统计项到达顺序将统计项入库，按data_id自增。
后台在每天的9点到17点，2小时一次检测新产生的data_id,根据mysql的自增顺序以及mysql默认的字母序去update统计项（data_id）的display_order。
同时，平台给用户提供自主排序的功能，在【配置管理】->【子项排序及删除】里操作，用户点击“保存”后，前端将id列表传回php后台，php后台根据id列表的顺序，重置这批data_id的display_order为（1,2,3,...）